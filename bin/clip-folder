#!/bin/bash -eu

# args not equal 2
if [ "$#" -lt 2 ]; then
  echo "Usage: $0 <prompt> <folder>"
  exit 1
fi

norm="$(echo "$2" | sed 's|/$||')"

docker run -it --rm --gpus all -v bash_clip_cache:/root/.cache -v "$norm":/workspace/images bash-clip python3 logit.py "$1" | sed '2,$s|^|'"$norm"'/|'
